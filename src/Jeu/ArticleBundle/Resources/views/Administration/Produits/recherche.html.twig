<form action="{{ path('rechercheArticle') }}" method="GET" name="test">
	<div class="pad">
		Rechercher un article par son nom : {{ form_widget(form.recherche) }}
		{{ form_widget(form) }}
	</div>
</form>